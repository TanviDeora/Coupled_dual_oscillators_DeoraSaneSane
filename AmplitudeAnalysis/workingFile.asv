for iRow = 1:height(t)
    disp(t{iRow, 1})
    disp(t{iRow, 2})
    
    % ... do work on r(iRow,:) ...
end

tcell = table2array(t(:,1))

%fields = {'field1','field2','field2'} 
%c = cell(length(fields),1);
%s = cell2struct(c,fields);